# 更新记录

## 未发布

- 发布依赖审计升级 `sharp` 至 0.35.4，修复旧版内置图像解析库的已公开高危漏洞；独立发布快照重新执行安装、构建和完整回归。
- 分离最终图层画幅与生成分辨率：155万像素以内的请求计划、超预算小字提示、图片与标注坐标映射；实际返回按1%比例容差等比放大/缩小，允许约157万像素及低于目标尺寸的输出。保留原始文件与适配信息，禁止自动裁透明包围盒；显式裁切保护所有非零 alpha，背景微小补边复制边缘并提示检查。
- 增加逐层暂存检查与文件复用，暂存不创建结果节点、不完成任务；合并图层及总图尺寸变化可重新计算生成计划。同步 MCP、执行技能、需求与使用文档，并增加生成尺寸、半透明裁切、背景补边、暂存和界面提示回归测试。
- 真实任务补充三通道不透明 PNG 背景兼容：不再固定读取不存在的 alpha 通道；新增对应回归。执行规则增加视觉位置验收，透明主体被生图工具自动裁紧、居中或改变占比时，即使尺寸校验通过也不得暂存为合格结果。

- Codex 预分层改为无像素框规划：只读取干净图片，提交名称、图层顺序、对象框和意见；服务拒收预览图片，更新自动派单、MCP 和技能中的阶段约束。真实生图留到用户确认以后。
- 所有预分层方案支持手动新增规划层、命名、拖框、意见与标注、排序、删除和撤销；保存恢复保留框与归属。规划层不伪装成图片，正式结果仍创建新节点；新增混合预览、局部合并及纯框重建回归检查。

- 冷审修复：局部选中多层合并时移除已合并的来源层，迁移标注并保持未选中层不变；下载/加载摘要校验改为兼容 Python 3.10 的分块实现，增加无网络回归。

- 整理当前需求与验收约束；统一前后端领域类型，拆分图层产物导入和标注绘制，统一核心源码排版。并发模型状态检查合并为一次，成功缓存 15 秒，失败可立即重试。
- 移除退役 SuperSVG/AdaVec 适配器、安装与实验脚本、ISNet 分支、旧参数校验和未引用的手动上传组件；保留旧设置迁移及现有网页回传接口。新安装不再下载退役模型；三个当前抠图模型和所有画布交互保持不变。
- Python 测试改用自建素材，取消私人示例和旧矢量输出依赖；增加模型状态并发与失败恢复测试。

- 统一所有分层模式的重建输入：预览仅服务于用户标注，从任务包移除预览图片路径，提供明确允许的干净素材列表；停用真实分层精抠准备入口。新增解码像素检查，拒收预览的改名或重编码副本，并禁止用原图抠字/像素拼回替代生成。

- 修正精细抠图定位：与粗抠一样仅作分层/物体标注参考，发丝等边缘可辅助判断，不锁定像素或蒙版。方案4/5改为 Codex 对照原图逐层重建，预分层默认标为重建，并拒收显式标记为预览的最终产物。

- Codex 重建、补图与 PS/AI 交接统一携带默认一致性原则：核对最初导入原图，保留已确认修改，按本次意见作用域修改，并检查实际合成结果。
- 修复重启 Codex 后同一对话画布看似变成空白新画布的问题：持久化本机访问凭证、画布视角和最后选中节点，并显示恢复的节点数量。

## 0.1.0 — 2026-09-19 首次公开源码发布

- Windows Codex 对话绑定的多图画布，亮暗主题及可调面板。
- 两种 PSD 预分层/生成重建流程，图层意见与图片坐标标注。
- 单节点操作生成新的来源关联结果；支持删除、框选、多选和标注标签。
- 官方对话工具自动发送、冻结任务输入、取消与版本冲突保护。
- 免费网页两种操作模式、三类付费 API 接入及 SVG 自动导回。
- PSD / SVG 导入导出、可编辑文字与支持的字效、可选 Adobe 原生文件交接。
- 恢复最初原图辅助参考，当前节点优先；只读原图对比开关。
- 统一工具 SVG 图标，节点连线由3px加粗为9px。
- 移除方案1与本地矢量模型入口；完整素材备份仍需 data 目录。
- 整理设计师教程、安装、排错、架构、智能体约定、开发及安全说明；排除本机私有配置和历史素材。

此前开发过程记录不作为新安装或当前功能的依据。当前状态以 README、需求边界和代码为准。

### 发布兼容性修复

- 中文 PowerShell 脚本采用 UTF-8 BOM，兼容系统自带 Windows PowerShell 5.1。
- 使用 Codex 随附 Node 时补齐安装进程 PATH，并检查 Node 22+。
- 矢量化上传素材及标注蒙版经 Node 文件读写，避免原生图像库的 Windows 长路径写入失败；新增超过260字符路径回归。

## 2026-09-19 · 本地直接精细抠图

- 新增 BiRefNet_HR-matting、Lucida v7 两个独立流程，保留原本2/3流程。
- 原图直接推理，保留软透明度；Codex 负责背景/遮挡补全。
- 粗抠与两款精细模型独立参数、用途与增减说明、恢复通用值和保存默认。
- 提交时冻结设置，精细准备工具验证图层身份与版本；完成仍创建新节点。
- 下载固定版本并核验摘要，优先国内镜像；更新安装、教程和工具契约。

## 2026-09-19 · 跨电脑安装加固

- Photoshop MCP 改为默认安装，Windows 脚本随包；明确 Illustrator 官方 MCP 是软件内置服务，需用户启用与配置。
- 统一 Node/Python/Codex/helper 探测、显式路径覆盖、CODEX_HOME 支持和只读预检；不要求相同用户名或安装盘。
- 原目录更新保护、可选组件失败隔离、安装报告和真实 PNG / MCP 工具自检。
- 修复 PowerShell 中文输出和 JSON BOM、npm lock 空键解析问题；默认端口冲突自动选择，防止连接到另一安装的服务。

## 2026-09-20 图层画幅

- 预分层增加独立画幅、拖边/角调整、拖标签移动、恢复建议和撤销。
- 新预览重建任务冻结每层画幅，回传按真实文件比例与尺寸校验、等比例适配；合并采用框并集并关联标注。
- 明确禁止先裁内容包围盒再拉伸、改色或删除半透明细节；粗抠和精抠仍只作结构标注。
- 保留旧数据和任务，不自动重建；视觉一致性仍需实际生图验收。
