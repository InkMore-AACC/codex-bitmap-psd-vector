import {z} from 'zod';
const finite=z.number().finite();const coord=finite.min(-100000).max(100000);const dimension=finite.min(1).max(30000);const str=z.string().max(20000);const id=z.string().min(1).max(200);const url=z.string().max(500);
const text=z.object({value:str,fontFamily:z.string().max(200),fontSize:finite.min(1).max(5000),color:z.string().max(100),x:coord,y:coord});
const effects=z.object({stroke:z.object({color:z.string().regex(/^#[0-9a-f]{6}$/i),size:finite.min(0).max(500)}).optional(),shadow:z.object({color:z.string().regex(/^#[0-9a-f]{6}$/i),blur:finite.min(0).max(500),offsetX:coord,offsetY:coord,opacity:finite.min(0).max(1)}).optional()});
export const layerSchema=z.object({id,name:z.string().max(300),url,previewUrl:url.optional(),x:coord,y:coord,width:dimension,height:dimension,visible:z.boolean(),opacity:finite.min(0).max(1),opinion:str,disposition:z.enum(['keep','rebuild']),kind:z.enum(['raster','text','vector']),svg:z.string().max(40000000).optional(),text:text.optional(),textDirty:z.boolean().optional(),psdStyle:effects.optional(),role:z.enum(['background','foreground']).optional(),preview:z.boolean().optional(),originalPsdIndex:z.array(z.number().int().nonnegative()).max(30).optional(),groupPath:z.array(z.string().max(200)).max(30).optional()});
const annotation=z.object({id,layerId:id.nullable(),type:z.enum(['arrow','ellipse','box','pen','text']),points:z.array(coord).min(2).max(20000).refine(p=>p.length%2===0),text:str,color:z.string().regex(/^#[0-9a-f]{3,8}$/i),opacity:finite.min(0).max(1).optional(),brushSize:finite.min(1).max(2000).optional(),strokeWidth:finite.min(1).max(20).optional(),fontWeight:finite.int().min(100).max(900).optional(),fontSize:finite.min(8).max(72).optional(),labelPosition:z.tuple([coord,coord]).optional()});
export const imageSchema=z.object({id,name:z.string().max(300),width:dimension,height:dimension,x:coord,y:coord,source:url,url,layers:z.array(layerSchema).max(300).refine(ls=>new Set(ls.map(l=>l.id)).size===ls.length,'图层 ID 不可重复'),vectorLayers:z.array(layerSchema).max(300).refine(ls=>new Set(ls.map(l=>l.id)).size===ls.length,'图层 ID 不可重复').optional(),annotations:z.array(annotation).max(2000),opinion:str,status:z.enum(['original','preview','layered','vector']),version:z.number().int().nonnegative(),vectorUrl:url.optional(),psdSource:url.optional(),parentId:id.optional(),sourceJobId:id.optional(),sourceVersion:z.number().int().nonnegative().optional(),basedOnOlderVersion:z.boolean().optional(),artifactUrl:url.optional()});
export const vectorOptionsSchema=z.object({supersvg:z.object({pathNum:z.number().int().min(64).max(4096).optional(),refinePathsPerSegment:z.number().int().min(1).max(64).optional(),refineBatchSize:z.number().int().min(1).max(64).optional(),seed:z.number().int().min(0).max(4294967295).optional(),optimizeIter:z.literal(0).optional(),device:z.enum(['auto','cpu','cuda']).optional()}).strict().optional(),adavec:z.object({segments:z.number().int().min(32).max(4096).optional(),compactness:finite.min(.1).max(100).optional(),colorMergeDistance:finite.min(.1).max(100).optional(),filterSpeckle:z.number().int().min(0).max(128).optional(),cornerThreshold:z.number().int().min(0).max(180).optional(),lengthThreshold:finite.min(3.5).max(10).optional(),spliceThreshold:z.number().int().min(0).max(180).optional(),pathPrecision:z.number().int().min(0).max(8).optional(),pathIterations:z.number().int().min(0).max(2000).optional(),shapeIterations:z.number().int().min(1).max(1000).optional(),seed:z.number().int().min(0).max(4294967295).optional(),device:z.enum(['auto','cpu','cuda']).optional()}).strict().optional()}).strict();
export const documentSchema=z.object({id,taskId:id,revision:z.number().int().nonnegative(),images:z.array(imageSchema).max(100).refine(images=>new Set(images.map(i=>i.id)).size===images.length,'节点 ID 不可重复'),settings:z.object({psdMode:z.union([z.literal(2),z.literal(3)]),vectorEngine:z.enum(['vectorizerCom','recraft','vectorizer302','vectorizer']),vectorizerComMode:z.enum(['auto','codex']).optional(),theme:z.enum(['dark','light']),vectorOptions:vectorOptionsSchema.optional()}),layout:z.object({layersWidth:finite.min(120).max(1000),opinionsWidth:finite.min(180).max(1400),editorHeight:finite.min(100).max(2000)}),updatedAt:z.string()});
export const jobSchema=z.object({imageId:id,useOriginal:z.boolean().optional(),useVectorLayers:z.boolean().optional(),layerIds:z.array(id).max(300).optional(),type:z.enum(['plan','layer','revise','vectorize','photoshop','illustrator']),mode:z.union([z.literal(2),z.literal(3)]).optional(),engine:z.enum(['vectorizerCom','recraft','vectorizer302','vectorizer']).optional()});
