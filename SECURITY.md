# 数据与安全说明

## 图片去哪里

画布素材、任务和备份默认保存在本机项目的 data/。使用 Codex 识别/生成/网页控制时，会通过相应 Codex 能力处理任务数据；使用矢量化网站或 API 时，会把选中的图片或图层上传给该服务。它不是“所有处理永远离线”的软件。敏感素材请先确认所选服务适用的数据条款。

API 凭据通过 Windows 当前用户 DPAPI 加密保存；不包含在项目 JSON 导出中。但 data/ 还包含会话令牌与私人素材，**整个目录都不能公开上传**。跨电脑备份需妥善保管，换账户需重新配置凭据。

带 `#token=` 的画布链接、service.json、Codex 桌面连接信息均不要公开。不要将本机服务监听地址改成 0.0.0.0；当前设计不是多人公网服务。

## 外部操作

浏览器验证由用户处理，不绕过验证码或网站限制。取消任务不能保证撤销已经发出的收费请求。Adobe 只操作当前任务指定副本或新输出，不能对不明身份的活动文档直接执行破坏性脚本。

## 报告漏洞

不要把真实密钥、私人图片或可用访问令牌贴进公开 Issue。可先提交不含敏感信息的简述，请维护者提供私下联系方式；若仓库提供 GitHub 私密漏洞报告入口，也可使用。当前未承诺响应时限或全面安全审计。

## 备份与删除

删除画布节点属于可撤销操作，底层素材和历史暂保留，不代表安全擦除。完整备份是 data/ 目录，单独项目 JSON 不含素材。未经用户要求，不自动清理其图片或历史。
